server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        http:
        grpc:

search_enabled: true

storage:
  trace:
    backend: local
    local:
      path: /var/tempo/traces
